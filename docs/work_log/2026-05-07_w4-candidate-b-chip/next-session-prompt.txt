W4-B-A 진입 — chip stateListAnimator <selector>-root callback bypass 정면 돌파.
직전 session commit 1 개 (`bf21dac` W4-B drawable XML feed mirror — candidate A
자연 trigger close + chip IT @Disabled park). 본 세션 첫 작업 = **W4-B-A 진입**
(layoutlib `Resources_Delegate.getAnimation_Original` 의 정확한 path 추적 +
plan v6 design 작성 + Round 7 Codex+Claude planning pair-review). OR user
priority 에 따라 W4-B-B 단독 (fixture-side stateListAnimator override 로 우회)
또는 W4-C/E (hold).

현 시점 상태 (commit `bf21dac`):
- 253 unit PASS / 0 fail
- 26 IT PASS + 1 SKIP (tier3 chip — @Disabled, LM-W4-B-A reference)
- T17 5/5 + T20 5/5 regression guards PASS
- acceptance gate `activity_basic` + `activity_minimal` PASS
- walker stats: `41 AARs (13 with res, 28 code-only, 191 color-state-lists,
  32 animator-xmls, 132 drawable-xmls)` — drawable feed wired in (W3D4-δ-D
  animator pattern 1:1 mirror)

W4-B drawable feed close 의 핵심:
- `AarResourceWalker.collectDrawableXmls` + `ParsedNsEntry.DrawableXml` +
  `NsBucket.drawables` + `LayoutlibResourceBundle.getDrawableXml` +
  `MinimalLayoutlibCallback` 의 `drawableXmlLookup` 5번째 ctor 인자 +
  `LayoutlibRenderer:198-203` callback wiring.
- 신규 unit test: `AarResourceWalkerDrawableTest` (4) + `LayoutlibResourceBundleDrawableTest` (6).
- `MinimalLayoutlibCallbackColorParserTest` 의 기존 "DRAWABLE → null (T12_5
  escalation 대상)" 테스트가 새 의미로 rename + sibling hit-case 추가.
- 영문화 (Rule 1 refactor-on-touch): `AarResourceWalker.kt` 의 skip-log 메시지
  + 그 assertion (AarResourceWalkerTest:43-45).
- dual-source review (feature-dev:code-reviewer): IMPORTANT 2 (chip IT KDoc 의
  Rule 2 phase-identifier 제거 + ColorParserTest class-level KDoc 의 stale
  DRAWABLE description 갱신) + MINOR 2 (AarResourceWalker + NsBucket KDoc
  refactor-on-touch 갱신) APPLIED. M1 (`COLOR_XML_SUFFIX` cross-domain naming)
  은 pre-existing broader-scope 으로 carry.

W4-B-A 의 정확한 fail surface:
- chip 의 inflation 이 `View.<init>:6033 → AnimatorInflater.loadStateListAnimator
  (context, id) → createStateListAnimatorFromXml(parser)` path 에서 fail
- `parser.next()` throw `XmlPullParserException: No Input specified
  (position:START_DOCUMENT null@0:0)`
- `m3_chip_state_list_anim` 이 bundle 에 등록되어 있음 (debugListAnimators
  probe 로 verify — 본 commit 에서 probe 는 revert 됨)
- 동일 path 의 `m3_btn_state_list_anim` 은 정상 working — basic IT PASS,
  `getParser type=animator name=m3_btn_state_list_anim` 정상 호출됨
- 차이점: chip animator XML 의 root 가 `<selector>` (직접 state list animator),
  button 은 `<set>` 안 `<selector>` (래핑)
- 가설: layoutlib `Resources_Delegate.getAnimation_Original` 가 root form 에
  따라 callback hook bypass

W4-B-B 의 정확한 fail surface (W4-B-A close 후 자연 surface):
- chip 의 stateListAnimator @null override 시 inflation advance — color state
  list lookup 정상 (m3_chip_assist_text_color, m3_ref_palette_*).
- `com.google.android.material.resources.TextAppearance.getTextSize()` NPE —
  `?attr/textAppearanceLabelLarge` 가 ChipDrawable.loadFromAttributes 의
  expected TextAppearance 로 resolve 안 됨. textAppearance instance == null.

Cold-read 6 docs (handoff §3.1 list, 이 순서):
1. `docs/work_log/2026-05-07_w4-candidate-b-chip/handoff.md` (본 prompt 의
   source — W4-B 의 close summary + W4-B-A/B carry)
2. `docs/work_log/2026-05-07_w4-candidate-b-chip/session-log.md` (W4-B drawable
   feed implementation 의 정확한 file diff + dual-source review 결과 + LM 산출)
3. `docs/work_log/2026-05-07_w4-candidate-d-hardening/handoff-w4-d-close.md`
   (W4-D 의 close + W4 candidate 카탈로그)
4. `docs/work_log/2026-05-06_w4-entry/handoff-w4-entry.md` (W4 entry candidate
   A/B/C/D/E spec)
5. `docs/work_log/2026-04-30_w3d4-beta-plumbing/session-log.md` (W3D4 phase
   escalation chain — background, candidate B 진입 시 escalation 패턴 reference)
6. `docs/work_log/2026-04-30_w3d4-beta-plumbing/round6-pair-review.md` (Round 7
   Codex+Claude pair-review 패턴 reference, W4-B-A 진입 시)

원본 기획안 (필수 cross-read):
- `docs/plan/03-roadmap.md` (master roadmap, historical/superseded by `06 §6`
  + `08 §1`)
- `docs/plan/04-open-questions-and-risks.md` (risk register R1-R9)
- `docs/MILESTONES.md` (W1-W6 gate 체크포인트, canonical schedule)
- `docs/superpowers/specs/2026-04-29-w3d4-material-fidelity-design.md`
  (W3D4 phase 의 design spec — Q1 mode 통일 + Q3 σ FULL chain walker +
  Q6 R name canonicalization)

진행:

0. 베이스라인 verify
   - cd server && ./gradlew test --console=plain
     → BUILD SUCCESSFUL — 253 unit / 0 fail
   - ./gradlew :layoutlib-worker:test -PincludeTags=integration --console=plain
     → 26 IT PASS + 1 SKIP (chip @Disabled)

1. W4-B-A vs W4-B-B 단독 vs hold — 사용자 priority 결정
   - W4-B-A (strong recommend, 정면 돌파, plan v6 + Round 7 pair-review)
   - W4-B-B 단독 (fixture-side override 로 우회, 단 LM-W4-B-A carry)
   - hold (다음 candidate 결정 — C/E)

2-A. W4-B-A 진입 (path 1 권장):
   - layoutlib jar dump (정확한 path 추적):
     ```
     find server/libs/layoutlib-dist -name '*.jar' | head
     unzip -l <layoutlib_jar> | grep -i 'Resources_Delegate\|ResourcesImpl_Delegate\|Resources\.class'
     unzip -p <layoutlib_jar> com/android/layoutlib/bridge/android/Resources_Delegate.class > /tmp/w4ba/Resources_Delegate.class
     javap -p -c /tmp/w4ba/Resources_Delegate.class > /tmp/w4ba/Resources_Delegate-disasm.txt
     ```
     (LM-W3D4-β-G unzip --directory /tmp/w4-* 의무 적용)
   - `getAnimation_Original` (또는 비슷한) 의 정확한 implementation 추적 —
     `<selector>` root 의 callback hook 우회 메커니즘 식별. file:line 인용 의무
     (LM-W3D4-δ-A + LM-W4-B-C 적용 — subagent 결론도 ground truth 아님,
     직접 verify).
   - 가능한 fix path:
     - (A) layoutlib 패치 — 비현실적 (jar 수정 거부)
     - (B) Worker-side ResourceType 별도 hook — `<selector>` root 의 selector
       기반 dispatch (ResourceType.STATE_LIST_ANIMATOR enum 미존재 → 다른 hook
       필요 — 가능성: `getParser` 가 호출되기 전에 BridgeContext 가 다른 callback
       method 를 호출하는지 확인)
     - (C) Pre-process `<selector>` root → `<set>` wrap — walker-side 변환
       또는 BridgeXmlBlockParser wrapper. cleanest fix 가능성.
   - plan v6 design 작성 (`docs/superpowers/specs/2026-05-XX-w4-b-stateListAnimator.md`):
     - §1 분석 (file:line 인용 — Resources_Delegate 의 정확한 lookup path)
     - §2 fix path 비교 (A/B/C trade-off + 회귀 risk + W3D4-δ-D animator
       pattern 과의 정합)
     - §3 12-step transition plan (W3D4 spec 의 §3.1 + §3.2 패턴)
     - §4 regression risk + recovery
     - §5 coverage 확장 (chip + Snackbar + Chip + 향후 Material widget 의
       selector-root stateListAnimator 사용 widget)
   - Round 7 Codex+Claude planning pair-review (LM-G + LM-W4-D-B 적용 — codex
     직접 CLI, model 명시 회피, reasoning effort 만):
     ```
     codex exec --skip-git-repo-check --sandbox danger-full-access \
       -c model_reasoning_effort=high \
       'Codex+Claude Round 7 plan-review prompt 본문...'
     ```
     pair-review prompt 에 Q1-Qn 외 Q6 open critique 슬롯 명시 (memory feedback
     `feedback_pair_review_q6_slot.md` 의무).
   - deltas inline → plan v6.x → GO
   - Implementation: 결정된 fix path 적용
   - regression guards 보장: T17 5/5 + T20 5/5 + W4-D require throw guard
     7 cases + drawable feed unit tests 4+6 + acceptance gate `activity_basic`
     + `activity_minimal` 모두 PASS
   - chip IT 의 @Disabled 제거 + LM-W4-B-A close
   - W4-B-B 자연 surface advance 시 그 phase 진입 OR 별도 next-session 으로
     carry

2-B. W4-B-B 단독 (W4-B-A 회피 — fixture override 유지):
   - `activity_chip.xml` 의 chip element 에 `android:stateListAnimator="@null"`
     추가 (fixture-side override)
   - chip IT 의 @Disabled 제거, 단 LM-W4-B-A 는 carry — 다른 selector-root
     animator widget 진입 시 다시 surface
   - textAppearanceLabelLarge resolve path trace + TextAppearance.create
     signature 확인
   - ChipDrawable.loadFromAttributes 의 정확한 TextAppearance 사용 path 추적
   - fix (gate 1-line 또는 작은 design)
   - 1 commit close — Material widget extension 의 visual fidelity 부분 진전

   본 path 는 W4-B-A 의 carry 를 유지 — 단기 가치 (chip IT green) 와 장기
   부채 (selector-root animator 의 일반 처리 미완성) 의 trade-off.

3. 본 phase commit + push (CLAUDE.md task-unit completion)

CLAUDE.md 규약 준수 (brace own-line, no default params, Zero Tolerance for
Magic Numbers, comments 영문 only / function-structure only / chronology
phrasing 회피 per Three Hard Rules). carry-forward LM (handoff §4 의 24
항목 — W3D4 19 + W4-D 2 + W4-B 3 — 모두 다음 세션 진입 시 내재화):

| LM | 1-line 핵심 |
|---|---|
| LM-W3D4-β-D~H | KDoc backtick + assertNotNull 금지 + IT-tag + subagent unzip /tmp + acceptance fail surface 분류 |
| LM-W3D4-γ-A~C | reviewer prompt + Long.decode + ThemeEnforcement multi-sentinel |
| LM-W3D4-δ-A | spec 코드 sample 의 모든 식별자 grep/Read 사전 verify |
| LM-W3D4-δ-B | NsBucket type-specific map 분리 sibling KILL POINT |
| LM-W3D4-δ-C | runtime-classpath.txt 부재 silent empty AAR — W4-D close ✓ |
| LM-W3D4-δ-D | IT-tagged RED-on-main 가능 (default unit suite 제외) |
| LM-W3D4-δ-E | acceptance fail surface 의 stack-trace entry-point 비교로 layer shift 식별 |
| LM-W3D4-δ-F | Rule 2 OUT-OF-SCOPE 확대 — phase/task identifier 회피 |
| LM-W3D4-δ-G | annotation message 안 future option 의 technical claim 의 file:line 사전 verify |
| LM-W3D4-δ-H | chain-walker IT (T17/T20-style probe) PASS 가 render-time path PASS 보장 안 함 |
| LM-W3D4-δ-I | RenderResources.applyStyle override 시 clear() 호출 금지 |
| LM (CLAUDE.md Three Hard Rules) | 모든 신규 코드 KDoc/inline 영문 only, function/structure only |
| LM-G | codex sandbox bypass 직접 CLI (planning phase 에만) |
| LM-W4-D-A | reviewer agent 가 신규 string literal + KDoc 의 modification-history phrasing 을 catch |
| LM-W4-D-B | Codex CLI 의 explicit `--model` flag 회피 (default model + reasoning effort 만) |
| LM-W4-B-A (carry) | chip stateListAnimator (@animator/m3_chip_state_list_anim) 가 callback.getParser 호출 없이 fail. button 은 정상. 차이는 root form (`<selector>` vs `<set>`). layoutlib `Resources_Delegate.getAnimation_Original` 의 root form 별 path 추적 + worker-side hook 보강 필요. 본 phase 의 진입 대상. |
| LM-W4-B-B (carry) | chip 의 stateListAnimator @null 시 textAppearance path 노출 — `?attr/textAppearanceLabelLarge` resolve 결과가 ChipDrawable.loadFromAttributes 의 TextAppearance instance==null 야기. W4-B-A close 후 진입 권장. |
| LM-W4-B-C | subagent (feature-dev:code-explorer 등) 의 가설은 hypothesis. 검증 없이 propagate 위험. file:line read 가 ground truth — 모든 subagent 결론은 직접 verify 후 plan 반영. |
| LM-W4-B-D | Kotlin helper function 에 default-arg 추가 시 trailing-lambda 호출 패턴 깨짐. 새 default-param 이 마지막 position 이면 trailing lambda binding 이 그 자리로 redirect. 두 form (1-arg + 2-arg) 분리 권장. |

본 phase 의 pair-review 적용:
- W4-B-A (정면 돌파): plan v6 작성 → Round 7 Codex+Claude planning pair-review
  필수 (LM-G + LM-W4-D-B — codex 직접 CLI, model 명시 회피, reasoning
  effort=high 만). pair-review prompt 에 Q6 open critique slot 명시 +
  feedback_pair_review_codex_killpoint.md 의 KILL POINT 패턴 적용.
- W4-B-B 단독: implementation phase — pair-review 강제 아님. 단 dual-source
  review (Claude reviewer subagent + Codex sanity-check) 권장 (W4-D 의 single-
  source 회피 패턴).

LM-G + LM-W4-D-B (codex sandbox bypass 직접 CLI, model 명시 회피) — planning
phase 진입 시에만 적용. 실 implementation 은 subagent-driven 가능
(LM-W3D4-β-G unzip --directory /tmp/w4-* 의무).

진행 후 candidate close 시 W4 cluster 의 work_log append + 다음 candidate
또는 새 phase 의 handoff/prompt 작성.

에이전트팀 구동하고 각종 스킬들 사용해서 철저하게 분석 하면서 진행해줘.
반드시 원본 기획안 (`docs/plan/03-roadmap.md`,
`docs/plan/04-open-questions-and-risks.md`, `docs/MILESTONES.md`,
`docs/superpowers/specs/2026-04-29-w3d4-material-fidelity-design.md`) 도
읽으면서 진행해야돼. mcp-builder, plugin-dev, mcp-server-dev 스킬은 본
phase 와 무관 — 사용 회피. plan v6 작성 phase 는 codex pair-review (LM-G +
LM-W4-D-B) 가 핵심. 새 세션에서 이어하는게 낫다고 판단되기 전까지 쭈욱
진행해줘.
